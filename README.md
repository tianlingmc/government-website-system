# 政府官网 / 裁判文书网系统

> [!WARNING]
> 项目已不再更新，强烈不建议使用本项目搭建对外站点！
> 本项目为技术学习演示作品，不建议、也不允许用于生产环境。

专业级政府官网 / 裁判文书网系统解决方案，基于 PHP8.1 + MySQL8.0 开发，采用 MVC 架构设计，支持前后台全功能。

---

## 系统功能

### 前台功能

| 模块 | 功能描述 |
|-----|---------|
| 🏠 **首页** | 现代化响应式设计，轮播图、快捷入口、公告政策展示、数据统计 |
| 📢 **政务公告** | 公告列表、详情查看、分类筛选 |
| 📄 **政策法规** | 政策文件展示、分类检索、文件下载 |
| ⚖️ **裁判文书** | 文书检索、全文搜索、分类筛选、附件下载 |
| 💬 **在线咨询** | 咨询投诉提交、处理状态查询 |
| 👤 **用户中心** | 用户登录/注册、个人资料、密码修改 |
| 🖼️ **懒加载** | 图片高斯模糊懒加载动画，提升页面性能 |
| 📱 **移动端** | 全站响应式适配，支持手机/平板访问 |

### 后台功能

| 模块 | 功能描述 |
|-----|---------|
| 📊 **控制台** | 数据统计仪表盘、快捷操作入口 |
| 📝 **内容管理** | 公告管理、政策法规、裁判文书、咨询投诉 |
| 👥 **用户权限** | 管理员管理、前台用户、角色权限分配 |
| 🖼️ **媒体库** | 文件上传管理、图片/视频/文档存储 |
| 🌐 **文字配置** | 前端所有文字后台自定义，无需改代码 |
| ⚙️ **网站设置** | 站点信息、SEO设置、联系方式、备案信息 |
| 🧭 **导航管理** | 前台导航菜单管理、排序、启用/禁用 |
| 📧 **SMTP配置** | 阿里云企业邮箱配置、邮件发送测试 |
| 💾 **数据备份** | 数据库备份、恢复、下载 |
| 📋 **操作日志** | 管理员操作记录、登录日志 |

---

## 技术栈

### 后端
- **PHP**: 8.1+
- **数据库**: MySQL 8.0+
- **缓存**: Redis（推荐）
- **架构**: MVC 分层架构
- **安全**: bcrypt 密码加密、SQL注入防护、XSS过滤

### 前端
- **HTML5** + **CSS3** + **JavaScript**
- **Bootstrap 5**: 响应式UI框架
- **AdminLTE 3**: 后台管理模板
- **Font Awesome**: 图标库
- **jQuery**: DOM操作

---

## 环境要求

### 系统要求
- Linux / Windows Server
- Nginx 1.20+ / Apache 2.4+
- PHP 8.1+
- MySQL 8.0+

### PHP扩展
```
pdo_mysql
gd
redis (推荐)
fileinfo
mbstring
openssl
```

---

## 部署方式

### 方式一：宝塔面板部署（推荐）

#### 1. 创建网站
- 登录宝塔面板
- 创建新网站，绑定域名
- 选择 PHP 版本 8.1

#### 2. 上传代码
将 `v1.0` 文件夹内所有文件上传到网站目录

#### 3. 设置运行目录
将网站运行目录设置为 `/public`

#### 4. 配置伪静态

**Nginx:**
```nginx
location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=$1 last;
    }
}

location ~ \.php$ {
    fastcgi_pass 127.0.0.1:9000;
    fastcgi_index index.php;
    include fastcgi_params;
}
```

**Apache:**
```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?s=$1 [QSA,PT,L]
</IfModule>
```

#### 5. 设置目录权限
```bash
chmod -R 755 runtime/
chmod -R 755 app/config/
chmod -R 755 public/media/
chmod -R 755 backup/
```

#### 6. 运行安装向导
访问 `http://您的域名/install/`，按步骤完成：
- 数据库配置
- 站点基本信息设置
- 创建超级管理员账号

---

### 方式二：手动部署

1. 上传代码到服务器
2. 配置 Web 服务器指向 `public` 目录
3. 配置伪静态规则（同上）
4. 设置目录权限（同上）
5. 创建数据库，导入 `database/install.sql`
6. 修改 `app/config/database.php` 配置数据库连接
7. 访问网站完成初始化

---

## 目录结构

```
v1.0/
├── public/              # 网站根目录（对外访问）
│   ├── index.php        # 前台入口
│   ├── admin.php        # 后台入口
│   ├── install/         # 安装向导
│   ├── assets/          # 前台静态资源（CSS/JS/图片）
│   ├── admin/           # 后台静态资源
│   └── media/           # 上传文件存储目录
│
├── app/                 # 应用目录
│   ├── config/          # 配置文件
│   ├── controller/      # 控制器（home前台/admin后台）
│   ├── model/           # 数据模型
│   └── view/            # 视图模板
│
├── core/                # 核心框架
│   ├── App.php          # 应用核心
│   ├── Controller.php   # 基础控制器
│   ├── Model.php        # 基础模型
│   └── ...
│
├── runtime/             # 运行时目录（日志/缓存）
├── database/            # 数据库文件
│   └── install.sql      # 数据库结构
├── backup/              # 数据备份目录
└── vendor/              # Composer 依赖
```

---

## 使用说明

### 后台访问
- 地址: `http://您的域名/admin.php`
- 账号: 安装时创建的管理员账号

### 文字自定义
所有前端显示文字均可在后台「文字配置」模块自定义，无需修改代码文件。

### 联系方式配置
前台显示的联系方式（电话、邮箱、地址、工作时间）在后台「网站设置」中统一配置。

### 媒体上传
- 支持格式: jpg, png, gif, mp4, pdf, doc, docx 等
- 文件自动哈希重命名，避免冲突
- 后台可查看、搜索、删除已上传文件

---

## 安全建议

1. [x] 安装完成后删除 `public/install` 目录
2. [x] 定期修改管理员密码
3. [x] 启用 HTTPS 访问
4. [x] 配置 Web 应用防火墙 (WAF)
5. [x] 定期备份数据库（可使用后台备份功能）
6. [x] 设置复杂的后台登录地址（可考虑修改 admin.php 文件名）

---

## 数据库表结构

| 表名 | 说明 |
|-----|------|
| `gov_admin` | 管理员表 |
| `gov_user` | 前台用户表 |
| `gov_admin_role` | 管理员角色表 |
| `gov_settings` | 网站设置表 |
| `gov_notice` | 公告表 |
| `gov_policy` | 政策法规表 |
| `gov_judicial` | 裁判文书表 |
| `gov_consult` | 咨询投诉表 |
| `gov_nav` | 导航菜单表 |
| `gov_logs` | 操作日志表 |
| `gov_backup` | 数据备份记录表 |

---

## 更新日志

### v1.0 (2026-04-06)
- [x] 初始版本发布
- [x] 完整前后台功能
- [x] 可视化安装向导
- [x] 裁判文书检索系统
- [x] 媒体库管理
- [x] 文字配置系统
- [x] 图片懒加载动画
- [x] 响应式前端设计

---

## 技术支持

如有问题或建议，欢迎反馈。

## 许可证

MIT License
